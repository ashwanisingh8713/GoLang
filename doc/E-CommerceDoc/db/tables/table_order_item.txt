# To create Table order_item

CREATE TABLE ecommerce.order_item (
item_id uuid,
order_id text,
quantity text,
address_id text,
product_id text,
updated timestamp,
create_date date,
PRIMARY KEY ((item_id), order_id, create_date) )
WITH comment = 'Q7. Find initiated orders by User';