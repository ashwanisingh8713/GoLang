# To create Table wishlist

CREATE TABLE ecommerce.wishlist (
wishlist_id uuid,
user_id text,
product_id text,
quantity int,
created_at timestamp,
PRIMARY KEY ((wishlist_id), user_id, product_id ) )
WITH comment = 'Q16. Find wishlist of User'
;