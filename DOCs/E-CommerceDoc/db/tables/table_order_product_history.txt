# To create Table wishlist

CREATE TABLE ecommerce.order_product_history (
order_product_history_id uuid,
user_id text,
order_id text,
product_quantity int,
created_at timestamp,
modified_at timestamp,
date date,
product_id text,
category_id text,
dimension text,
manufacturer_id text,
name text,
picture text,
price decimal,
description text,
seller_id text,
sku text,
sub_category_id text,
weight decimal,
units  int,
subscribable boolean,
seller_name text,
about_product text,
PRIMARY KEY ((wishlist_id), user_id, product_id, date) )
WITH comment = 'Q17. Find order history of the User'
;